{
  "name": "gasguard",
  "version": "1.0.0",
  "private": true,
  "description": "GasGuard monorepo",
  "scripts": {
    "dev": "pnpm --filter @gasguard/web dev",
    "build": "pnpm --filter @gasguard/core build && pnpm --filter @gasguard/web build",
    "test": "pnpm --filter @gasguard/core test && pnpm --filter @gasguard/web test"
  },
  "packageManager": "pnpm@10.33.0"
}
